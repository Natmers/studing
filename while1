#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    double x, a = -6, b = 6, h = 0.5;
    x = a;
    while (x < b) {
        cout << "x: " << x << " y: " << cos(2 * x) - (x * x) / 5 << endl;
        a += h;
    }
}
