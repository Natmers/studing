#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    double x = -6, b = 6, h = 0.5;
    while (x < b) {
        cout << "x: " << x << " y: " << cos(2 * x) - (x * x) / 5 << endl;
        x += h;
    }

    return 0;
}
